set(binary_resolution).
clear(print_initial_clauses).
clear(print_kept).
clear(print_given).
assign(max_seconds,1).
assign(stats,none).

formulas(sos).
end_of_list.

formulas(goals).
        has_class_3_certificate(user,certificate) -> can_call_token_api(user,certificate).
end_of_list.
